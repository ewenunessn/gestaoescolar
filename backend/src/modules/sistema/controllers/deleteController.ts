import { Request, Response } from 'express';

export const deleteInstitution = async (req: Request, res: Response) => {
  // Stub - implementar deleção de instituição
  res.status(501).json({ message: 'Not implemented' });
};
