{
  "name": "gestaoescolar",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "echo 'Serverless API - No build needed' && mkdir -p public && echo '{\"api\":\"serverless\"}' > public/index.json"
  },
  "workspaces": [
    "api",
    "backend"
  ]
}
