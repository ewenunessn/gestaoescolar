{
  "name": "gestao-escolar",
  "version": "1.0.0",
  "description": "Sistema de Gerenciamento de Alimentação Escolar",
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "build": "cd frontend && npm run build",
    "dev": "concurrently \"cd backend && npm run dev\" \"cd frontend && npm run dev\""
  },
  "devDependencies": {
    "@types/node": "^24.3.1",
    "concurrently": "^8.2.2"
  },
  "dependencies": {
    "emailjs-com": "^3.2.0",
    "@vercel/node": "^3.0.0"
  }
}
